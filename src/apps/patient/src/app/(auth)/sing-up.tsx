import { Text, View } from "react-native";
import { Container } from "@/components/container";

const SingUp = () => {
	return (
		<Container>
			<View>
				<Text>Sign in</Text>
			</View>
		</Container>
	);
};

export default SingUp;
